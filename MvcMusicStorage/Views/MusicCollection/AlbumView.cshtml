@model IEnumerable<MvcMusicStorage.Models.Album>

@foreach (var album in Model)
{
    <div class="album-block">
        <img src="@album.ImgUrl" width="150" height="150" />
        <div class="album-name">@album.Name</div>
        <div>@album.Year</div>
    </div>
}
</div>